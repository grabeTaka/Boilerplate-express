info:
  name: create
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3333/api/products
  body: json
  auth: inherit

body:
  json: |-
    {
      "name": "New product"
    }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
